# 1. 의존성 폴더 (용량이 크고 필요 없음)
node_modules/

# 2. 환경변수 및 비밀 정보 (가장 중요! ⭐)
.env
.env.local
.env.*.local
*.pem
*.key

# 3. 빌드 결과물 (자동 생성되는 파일들)
/build
/dist
.next/
out/

# 4. OS 시스템 파일 (지저분함)
.DS_Store
Thumbs.db

# 5. IDE 설정 파일 (나만 쓰는 설정)
.vscode/
.idea/

# 6. 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*